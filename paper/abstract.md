# Abstract

Source of truth: `paper/sections/abstract.tex`, included by `paper/main.tex`.

The LaTeX abstract frames the paper as a controlled return-support audit plus a CPU-light CartPole-v1 benchmark tier. Candidate-count pressure raises tail pressure on the proxy score `S`; under out-of-support return conditioning, the selected proxy return rises while real utility `R` falls. The abstract reports the headline synthetic numbers (`S: 9.299 -> 9.468`, `R: 7.825 -> 7.381`), the expanded `N = 256` repair evidence, and the CartPole-v1 raw-versus-support-aware benchmark result.
