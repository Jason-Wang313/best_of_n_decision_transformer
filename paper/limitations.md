# Limitations

- The learned model is intentionally small and should be described as DT-style rather than a benchmark-scale Transformer.
- The environment is synthetic and designed for mechanism clarity.
- The support estimator is simple.
- Pilot calibration assumes access to a small number of real-utility labels.
- The oracle selector is a diagnostic upper bound, not a deployable method.
- Benchmark validation remains future work.

